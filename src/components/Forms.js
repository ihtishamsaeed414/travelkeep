export default function Form() {
  return (
    <form className="add-form">
      <h3>what do you need for your trip?</h3>

      <select>
        <option></option>
      </select>

      <input type="text" />

      <button>ADD</button>
    </form>
  );
}
